import { SortRequestVirtualMachines } from './sort-request-virtual-machines';

describe('SortRequestVirtualMachines', () => {
  it('should create an instance', () => {
    expect(new SortRequestVirtualMachines()).toBeTruthy();
  });
});
